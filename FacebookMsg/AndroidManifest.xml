<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nxp.facebookmsg"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.NFC" />

    <uses-sdk
        android:minSdkVersion="12"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="com.nxp.facebookmsg.AboutScreen"
            android:theme="@android:style/Theme.Holo"/>

        <activity
            android:name="com.nxp.facebookmsg.ReadAndWriteScreen"
            android:theme="@android:style/Theme.Holo"/>

        <activity
            android:name="com.nxp.facebookmsg.MessageScreen"
            android:theme="@android:style/Theme.Holo"/>

        <activity
            android:name="com.nxp.facebookmsg.CreateAndModifyScreen"
            android:theme="@android:style/Theme.Holo"/>

        <activity
            android:name="com.nxp.facebookmsg.MainScreen"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Holo">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>
    </application>

</manifest>